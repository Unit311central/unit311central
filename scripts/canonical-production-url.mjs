/**
 * Canonical Unit311 Central production origins (single Vercel project: unit311central).
 * Re-exports from assert-canonical-unit311-repo.mjs — single source of truth.
 */
export {
  CANONICAL_DEMO_ORIGIN,
  CANONICAL_INTERNAL_ORIGIN,
  CANONICAL_PRODUCTION_ORIGIN,
  CANONICAL_PUBLIC_ORIGIN,
  CANONICAL_SITE_HOST,
} from "./assert-canonical-unit311-repo.mjs";
