/**
 * Re-export bootstrap for explicit app init (optional).
 * Registry auto-bootstraps on first lookup via ensureIntelligencePacksBootstrapped().
 */
export { bootstrapIntelligenceWorkspacePacks } from "@/lib/intelligence/workspace-packs";
