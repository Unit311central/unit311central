/** Public shell for OnwardAir client portal routes (login + authenticated app). */
export default function OnwardAirClientPortalRootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
