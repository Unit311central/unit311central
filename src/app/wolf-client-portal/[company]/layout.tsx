/** Public shell for WOLF Central client portal routes (login + authenticated app). */
export default function WolfClientPortalRootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
